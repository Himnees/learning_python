# Arthematic operators
a = 10
b = 5
c = 4
print("Sum = ",(a+b)) # uses + operator
print("diff = ",(a-b)) # uses - operator
print("product = ",(a*b)) # uses * operator
print("div = ",(a/b)) # uses / operator and returns in float type
print("div = ",(a/c)) # uses / operator and returns in float type
print("floor div = ",(a//b)) # uses // operator and returns in int type
print("floor div = ",(a//c)) # uses // operator and return in int type